# Apple MusicKit provider package

This package defines the SDK v1 per-user playlist boundary. The in-core adapter remains active until SDK playlist artwork parity and atomic provider switchover are complete.
