# Spotify provider package

This package defines the SDK v1 account, permission, and playlist boundary. The in-core adapter remains active until the SDK playlist artwork hook and atomic provider switchover are complete.
