# Code Map For The Overhaul

Use this file to ground overhaul tasks in current code. The root plan is [OVERHAUL.md](../../../OVERHAUL.md).

This began as the Phase 0 legacy-code assessment and now records the current ownership and remaining
compatibility dispositions. Current-behavior cells are authoritative; historical migration wording is retained
only where it explains why a compatibility path still exists.

## Disposition Terms

- **Keep**: preserve the current ownership and public behavior.
- **Wrap**: put a typed boundary around current behavior before moving internals.
- **Refactor**: improve the current implementation in place while preserving its boundary.
- **Replace**: migrate callers behind a tested adapter, then remove the old implementation.
- **Retire**: remove behavior that is unsafe, dead, misleading, or unsupported; do not carry it into the target core.

## Existing Steering

- [docs/steering/INTRODUCTION.md](../INTRODUCTION.md): repo shape and high-level editing guardrails.
- [docs/steering/ARCHITECTURE.md](../ARCHITECTURE.md): host, middleware, controllers, services, runtime state.
- [docs/steering/PROVIDERS.md](../PROVIDERS.md): current metadata provider contract and provider rules.
- [docs/steering/DOWNLOADS.md](../DOWNLOADS.md): download behavior and file handling guidance.
- [docs/steering/BACKENDS.md](../BACKENDS.md): backend compatibility expectations.
- [docs/steering/CACHING.md](../CACHING.md): cache behavior and persistence.
- [docs/steering/SCROBBLING.md](../SCROBBLING.md): Last.fm and ListenBrainz behavior.
- [docs/steering/TESTING.md](../TESTING.md): test suite shape and commands.

## Current Subsystem Assessment

| Subsystem and current anchors | Current behavior and ownership | Disposition | Migration risk, coverage, and rollback |
| --- | --- | --- | --- |
| ASP.NET host and listener split: [Program.cs](../../../allstarr/Program.cs) | One host owns proxy port `8080` and admin port `5275`. `BackendControllerFeatureProvider` registers exactly one catch-all protocol controller per deployment. | **Keep.** This is the control-plane boundary; a future simultaneous-protocol host needs a separate decision. | `HostCompositionTests` starts both backend modes without live traffic, asserts one protocol surface, and activates every controller. Roll back individual registrations without changing the listener contract. |
| Middleware and admin boundary: [Middleware](../../../allstarr/Middleware), [Filters](../../../allstarr/Filters) | Middleware owns bot blocking, always-redacted request logging, websocket relay, admin-network/static/auth isolation, and exception handling. `JellyfinAuthFilter` verifies non-public controller requests against backend `Users/Me`, then the protocol execution context resolves any linked Allstarr identity. | **Keep and refactor in place.** Preserve ordering and the backend-authenticated identity boundary. | Reordering can expose the admin surface or execute synthesized work before auth. `RequestLoggingMiddlewareTests`, admin boundary tests, `WebSocketProxyMiddlewareTests`, and route fixtures are rollback anchors. |
| Jellyfin controller and proxy: [JellyfinController*.cs](../../../allstarr/Controllers), [JellyfinProxyService.cs](../../../allstarr/Services/Jellyfin/JellyfinProxyService.cs) | Explicit auth, search, external item/image/audio, favorite, playlist, lyrics, playback, and catch-all paths mix protocol shaping with core/provider calls. Proxy results now preserve upstream JSON status codes. | **Wrap, then extract.** Keep catch-all compatibility while protocol actions move behind a Jellyfin adapter and shared execution context. | Highest client-compatibility risk. OpenAPI/source locks, `ProtocolRouteFixtureTests`, `ProxyResponseResultFactoryTests`, and existing Jellyfin response/search tests define rollback behavior. The current verifier supplies only a backend principal ID, not an Allstarr identity or provider account. |
| Subsonic/OpenSubsonic controller stack: [SubSonicController.cs](../../../allstarr/Controllers/SubSonicController.cs), [Services/Subsonic](../../../allstarr/Services/Subsonic) | Explicit search, stream, item, cover-art, playlist, playback, and star paths sit beside a generic relay. Native credentials are verified before synthesized work, and characterized relays preserve method, query/form source, repeated ordering, body, content type, and upstream status. | **Keep explicit adapters and characterized relay behavior.** Use one shared provider core rather than a second provider stack. | Auth-boundary, relay-fidelity, playlist, playback, and parser/builder/proxy fixtures are rollback anchors. |
| Proxy result/error shaping: [ProxyResponseResultFactory.cs](../../../allstarr/Controllers/ProxyResponseResultFactory.cs), [Helpers.cs](../../../allstarr/Controllers/Helpers.cs) | Controller helpers translate upstream JSON or empty responses while retaining the upstream HTTP status. | **Keep.** Reuse it while extracting adapters; extend only for characterized content types and headers. | `ProxyResponseResultFactoryTests` protects JSON success/error and empty-response behavior. Do not broaden it into provider routing. |
| Admin/config/diagnostics controllers: [Controllers](../../../allstarr/Controllers), [Services/Admin](../../../allstarr/Services/Admin) | Admin routes aggregate deployment and compatibility configuration, durable runtime settings, cache/status, playlists, matching, extensions, downloads, intelligence, and scrobbling. | **Keep `/api/admin` and port isolation.** New runtime preferences use durable settings and provider secrets use encrypted accounts; `.env` writes remain compatibility-only. | Authorization and secret masking are the main risks. Admin auth/config tests characterize current behavior; pre-overhaul startup state is not imported. |
| Legacy provider metadata/download convergence: [MultiProviderMetadataService.cs](../../../allstarr/Services/Common/MultiProviderMetadataService.cs), [MultiProviderDownloadService.cs](../../../allstarr/Services/Common/MultiProviderDownloadService.cs) | Compatibility paths still converge through legacy metadata and download services. Typed paths use capability-specific contracts and `ProviderRouter`. | **Retain only for characterized compatibility.** New work belongs in the typed capability core. | Do not add a second router. Provider service and fake-provider route-decision tests protect both boundaries until compatibility callers are retired. |
| Provider enablement and runtime status: [ProviderStatusManager.cs](../../../allstarr/Services/Common/ProviderStatusManager.cs), [ProviderRuntimeStatus.cs](../../../allstarr/Services/Common/ProviderRuntimeStatus.cs) | The compatibility projection separates configured state from in-memory `Unknown`, `Testing`, `Healthy`, or `Degraded` observations. Typed routes use durable account/capability samples, rollups, circuits, and resolved accounts. Disabled providers leave every lane, and SquidWTF is metadata-only. | **Keep the compatibility projection; use durable health and account policy for typed routes.** | `ProviderStatusManagerTests` covers pure compatibility reads and probe isolation. `Unknown` may be attempted only on eligible compatibility paths and is never reported ready. |
| Built-in provider adapters: [Services/AppleMusic](../../../allstarr/Services/AppleMusic), [Services/Deezer](../../../allstarr/Services/Deezer), [Services/Qobuz](../../../allstarr/Services/Qobuz), [Services/SquidWTF](../../../allstarr/Services/SquidWTF) | Current services expose uneven metadata, download-backed streaming, download, playlist-read, and health behavior through global configuration. | **Wrap individually.** Publish only capabilities listed by `CurrentProviderSupportCatalog`; replace internals only behind common typed contracts. | Provider-specific fake HTTP tests are rollback anchors. Missing download credentials must not suppress public metadata. A capability must not inherit another capability's failed probe. |
| SquidWTF stream/download paths | Legacy download/stream classes remain present, but current status/routing excludes SquidWTF from stream, download, and playlist lanes. | **Retire from current routing; retain metadata adapter.** Reintroduce another capability only with a working endpoint and contract fixture. | `ProviderStatusManagerTests` guards the quarantine. The optional uptime source is discovery information, not proof of a working media endpoint. |
| Optional Apple download gateway: [AppleMusicController.cs](../../../allstarr/Controllers/AppleMusicController.cs), [Apple typed adapter](../../../allstarr/Core/Providers/AppleDownload), [gateway](../../../sidecars/apple-gateway) | The admin API calls the private optional gateway, preserves 2FA/pending statuses, and strips tokens. The typed adapter verifies song artifacts in host-owned workspaces. | **Keep the HTTP boundary and optional profile.** Advertise only capabilities its contract and health probe verify. | Controller and fake-gateway tests protect login, discovery, streamed artifact, MIME, size, checksum, and ownership behavior. |
| Playlist and matching stack: [Core/Playlists](../../../allstarr/Core/Playlists), [Core/Matching](../../../allstarr/Core/Matching), plus legacy [Services/Spotify](../../../allstarr/Services/Spotify) and [JellyfinController.Spotify.cs](../../../allstarr/Controllers/JellyfinController.Spotify.cs) | New Spotify and Apple MusicKit sources are account-bound and provider-neutral. Durable snapshots, matches, virtual reads, schedules, and Jellyfin/Subsonic materialization use the core. The specialized Redis mapping/injection path remains compatibility-only. | **Keep the provider-neutral core; retire legacy compatibility after its fixtures and routes are removed deliberately.** | Playlist orchestration, target, virtual protocol, scanner, tenant-isolation, WebUI, and mapping-review tests cover the new path. Fresh installs do not import legacy cache mappings. |
| Lyrics, playback, and scrobbling: [Services/Lyrics](../../../allstarr/Services/Lyrics), [Services/Scrobbling](../../../allstarr/Services/Scrobbling), [Core/Playback](../../../allstarr/Core/Playback) | Lyrics sources remain orchestrated services. Authenticated playback observations and optional Last.fm or ListenBrainz delivery now cross a durable, idempotent job boundary with exact actor scope. | **Keep the provider services behind the durable core.** Preserve native protocol results, dedupe, scoped credentials, retention, and sanitized failures. Do not add controller fire-and-forget work. | Playback pipeline, scrobbling orchestrator, protocol, and admin tests cover thresholds, retries, scope, and failure shaping. Do not log raw upstream bodies or tokens. |
| Recommendations and generated sets: [Core/Intelligence](../../../allstarr/Core/Intelligence), [IntelligenceController.cs](../../../allstarr/Controllers/IntelligenceController.cs) | Exact-scope policy controls retained signals, habit profiles, provider readiness, recommendation runs, explanations, and generated playlists. Jellyfin and Subsonic materializers reconcile exact local matches through shared playlist targets. | **Extend through typed sources and targets.** Keep MusicBrainz claims limited to local enriched relationships, require explicit Subsonic credentials, and treat AudioMuse as optional until healthy and contract-compatible. | Intelligence, provider-source, controller, and generated-set materializer tests cover opt-in, purge, seeds, isolation, ordering, skips, retries, and cancellation. |
| Extension install/runtime: [ExtensionManager.cs](../../../allstarr/Services/Common/ExtensionManager.cs), [ExtensionController.cs](../../../allstarr/Controllers/ExtensionController.cs) | SDK v1 validates checksums, manifests, permissions, archive bounds, deterministic content hashes, staged activation, disable/update/rollback, and typed account-scoped hooks. No registry is auto-added and remote staging remains default-deny. JavaScript runs in constrained in-process Jint, not an OS sandbox. | **Keep the verified lifecycle; harden runtime isolation in a future SDK revision.** First-party packages must pass the same lifecycle and routing checks. | SDK, lifecycle, security, capability-adapter, control-plane, and first-party package tests cover this boundary. Never describe in-process execution as an operating-system sandbox. |
| Local library, external IDs, and mapping: [Services/Local](../../../allstarr/Services/Local), [PlaylistIdHelper.cs](../../../allstarr/Services/Common/PlaylistIdHelper.cs), [Core/Matching](../../../allstarr/Core/Matching) | Local lookup and typed/legacy external IDs support both protocol controllers. Tenant/library-scoped canonical recordings, provider identities, and match decisions are durable; file/env/cache mappings remain compatibility inputs. | **Keep route ID compatibility and durable matching authoritative.** | Mapper, ID, injected-playlist, and local-library tests preserve client behavior, not legacy persisted mapping import. Never reinterpret an opaque provider ID through another provider without a verified identity link. |
| Fuzzy/track matching: [FuzzyMatcher.cs](../../../allstarr/Services/Common/FuzzyMatcher.cs), [SpotifyTrackMatchingService.cs](../../../allstarr/Services/Spotify/SpotifyTrackMatchingService.cs), [Core/Matching](../../../allstarr/Core/Matching) | Durable provider-neutral decisions are explainable and versioned. Heuristic helpers and Spotify background work remain on compatibility paths. | **Keep `TrackIdentityService` authoritative.** Retain legacy helpers only for characterized callers until those routes are deliberately retired. | Existing fuzzy/mapping tests preserve compatibility, not proof of cross-provider identity. New work uses durable match decisions and manual overrides. |
| Favorites and managed files: [Core/Favorites](../../../allstarr/Core/Favorites), [Core/ManagedFiles](../../../allstarr/Core/ManagedFiles), [Core/Enrichment](../../../allstarr/Core/Enrichment), [FavoriteActionPoliciesController.cs](../../../allstarr/Controllers/FavoriteActionPoliciesController.cs), [ManagedFilesController.cs](../../../allstarr/Controllers/ManagedFilesController.cs) | Favorite work is a durable, idempotent event/action/job chain with exact tenant, user, backend, library, and policy scope. Provider download artifacts, managed-file ownership, placement facts, enrichment plans/applications, and backend refresh results are durable. Unfavorite is logical and file removal is a separate confirmed action. | **Keep side effects behind the saved policy and managed ownership boundary.** Do not restore controller `Task.Run`, implicit deletion, or Redis-owned kept-file state. | Favorite pipeline/policy, placement, download artifact, enrichment, refresh, state-transfer, managed removal, restart, path attack, tenant isolation, and fake Jellyfin/Subsonic tests cover the current path. |
| Download activity and local playback metadata: [DownloadActivityController.cs](../../../allstarr/Controllers/DownloadActivityController.cs), [PlaybackActivityContracts.cs](../../../allstarr/Services/Common/PlaybackActivityContracts.cs) | Admin download activity consumes backend-neutral sources/resolvers. Jellyfin adapters resolve item metadata/artwork behind the admin boundary with bounded cached artwork. Subsonic can start without Jellyfin session services. Durable playback observations use the job boundary. | **Keep the seam and backend adapters.** | `DownloadActivityControllerTests`, `JellyfinPlaybackMetadataResolverTests`, playback pipeline tests, and host-composition tests protect activation, safe artwork, durability, and backend neutrality. |
| Cache and current runtime persistence: [RedisCacheService.cs](../../../allstarr/Services/Common/RedisCacheService.cs), [RedisPersistenceService.cs](../../../allstarr/Services/Common/RedisPersistenceService.cs), `.env`, `/app/cache` | PostgreSQL owns durable settings, accounts, mappings, extensions, sessions, jobs, and workflow state. Redis/Valkey and cache snapshots accelerate compatibility reads only; `.env` is bootstrap/deployment input, not runtime state. PostgreSQL is the sole durable database. | **Keep cache-compatible seams while retiring remaining legacy readers.** | Fresh installs do not backfill legacy cache/runtime files. Never treat SQLite, Redis/Valkey snapshots, or process memory as a runtime outbox, mapping store, or session database. |
| Hosted maintenance and background work: [Program.cs](../../../allstarr/Program.cs), [Services/Common](../../../allstarr/Services/Common), [Services/Spotify](../../../allstarr/Services/Spotify), [Core](../../../allstarr/Core) | Startup validation, cache warming/cleanup/persistence, Spotify compatibility fetch/match, durable jobs/outbox, favorite actions, library refresh, provider health, and sidecar monitoring run in process. Automatic pre-overhaul environment, favorite, mapping, and version-state migration services were retired. Optional provider startup probes are off by default and live validators do not run in `Testing`. | **Keep mutations leased, durable, scoped, and idempotent.** Keep the fresh-install boundary instead of adding startup imports. | Host and worker tests use temporary state and fake HTTP so CI never contacts live providers or backend libraries. |
| WebUI and schema: [wwwroot](../../../allstarr/wwwroot), [AdminUiSchema.cs](../../../allstarr/Models/Admin/AdminUiSchema.cs), [CurrentProviderSupportCatalog.cs](../../../allstarr/Services/Common/CurrentProviderSupportCatalog.cs) | The Lit UI consumes an admin schema and shows the current provider support matrix, distinct configuration/observed-health states, compact support chips, responsive grids/tables, and keyboard-operable mobile navigation. | **Refactor in place.** Keep current workflows; add sections only with durable APIs and complete states. | `JavaScriptSyntaxTests`, `CurrentProviderSupportCatalogTests`, and `WebUiResponsiveContractTests` are static regression anchors. Browser functional, focus, narrow-screen, and accessibility validation remain release checks. |
| Compose and optional services: [docker-compose.yml](../../../docker-compose.yml), [docker-compose.aio.yml](../../../docker-compose.aio.yml), [docker-compose.apple.yml](../../../docker-compose.apple.yml) | The fresh standard stack is core/Postgres/Valkey. AIO mounts only verified first-party packages. Apple remains an explicit source-locked profile requiring operator-supplied legal libraries. | **Keep the default stack small and optional profiles turnkey.** Optional endpoints need an explicit contract, health gate, and resource boundary. | Compose and gateway contract tests validate the profiles without a live account. Never hide a storage-provider change in an override. |
| Test and fixture infrastructure: [allstarr.Tests](../../../allstarr.Tests), [Fixtures/Protocols](../../../allstarr.Tests/Fixtures/Protocols) | xUnit covers current helpers, services, controllers, real-host composition, protocol source locks/support matrix, route/auth fixtures, provider status, extensions, redaction, file safety, and UI contracts. | **Keep and expand.** Fixtures are compatibility inputs, not generated promises. | `dotnet test allstarr.sln` is the minimum gate. No automated test may require a live provider account, backend library, or provider traffic. |

## Source And Fixture Anchors

- [protocol-source-lock.json](../../../allstarr.Tests/Fixtures/Protocols/protocol-source-lock.json): pinned Jellyfin OpenAPI hash/version plus octo-fiesta and Last.fm reference commits.
- [protocol-support-matrix.json](../../../allstarr.Tests/Fixtures/Protocols/protocol-support-matrix.json): endpoint-level current status, target, auth boundary, fixture, test location, and known gap.
- [ProtocolRouteFixtureTests.cs](../../../allstarr.Tests/ProtocolRouteFixtureTests.cs): real-host protocol behavior with fake upstream HTTP.
- [HostCompositionTests.cs](../../../allstarr.Tests/HostCompositionTests.cs): both deployment modes and controller activation.
- [CurrentProviderSupportCatalog.cs](../../../allstarr/Services/Common/CurrentProviderSupportCatalog.cs): visible current provider/capability truth, with `CurrentProviderSupportCatalogTests` guarding unsupported claims.

## Migration Rule

When moving code into the new architecture, prefer this order:

1. Assess the current service: public behavior, callers, state, tests, coupling, and whether each part should be kept, wrapped, refactored, replaced, or retired.
2. Add characterization tests around current behavior.
3. Add an adapter around the current service.
4. Route one capability through the adapter.
5. Move or replace internals behind the adapter in small, reviewable slices.
6. Delete duplicate or retired paths only after tests prove equivalence and the rollback window has passed.

Do not rewrite a subsystem merely because its structure is old. Prefer the smallest change that improves ownership, readability, testability, and debugging; escalate to a broader replacement only when the assessment shows that incremental migration cannot safely reach the target contract.
