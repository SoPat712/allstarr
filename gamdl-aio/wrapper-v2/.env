# wrapper-v2 environment for macOS Apple Silicon
# Build stage is always linux/amd64 (Google NDK is x86_64-only)
# Runtime is linux/arm64 for native performance on Apple Silicon
HTTP_PORT=8080
TARGET_ARCH=arm64-v8a
BUILD_PLATFORM=linux/amd64
RUNTIME_PLATFORM=linux/arm64
